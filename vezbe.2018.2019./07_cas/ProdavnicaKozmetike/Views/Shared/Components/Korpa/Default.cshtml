@model Korpa

<div class="">
    @if (Model.listaProizvodaUKorpi.Count() > 0)
    {
        <small class="navbar-text">
        <b>Vaša korpa:</b>
        @Model.listaProizvodaUKorpi.Count() proizvoda, 
        @Model.IzracunajVrednost().ToString("c", Cultures.Srbija)
        </small>
    }
    <br>
    <a asp-controller="Korpa" asp-action="SpisakKorpe" 
       asp-route-url="@ViewContext.HttpContext.Request.PathAndQuery()">
        <i class="fas fa-shopping-cart"></i>
    </a>

</div>